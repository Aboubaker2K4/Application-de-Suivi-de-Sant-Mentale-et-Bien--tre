Bonjour 
Routes :
 web.php il peut permet de defenir tout les routes li ymkn l'utilisateur ydkhle lihome  , on utilise sur on travailprojet fullstuck 
 api.php si on travail sur un projet separes (exemple frontend b react wa backend b laravel ) , 
 kanmchi fichier routes on doit insataller les routes li bghina avec *** php artisan install:api**
 les verbes HTTP , put ( la bghina ndiro mettre a jour total ) , patch ( bghina ndrio mettre a jour partielle )
 comment on defenir les chemins ; 
 ana f web.php {
     hado ga3e les controlleurs li 3ndna wa chaque verbes on doit utiliser : (
    index / Get
    create / GEt
    store / Post
    show / Get ( kanzido variable li kayne f show , kanzidohe f chemin /category/{smiya dyal variable })
   edit / GEt
   update / Put ou Putch  /variable/edit
   destroy / delete 
     )
     Route::get("/categories" , [CategoryController::class , "index"(hado les methodes)]);=>name("categories.index") 
     simplifiiter : Route::resource("categories" , categorycontroller::class) ; smiya nafsse l7aja kadirha resousrcer "categories.index"
     interface graphique (blades ): 
     c'est un moteur de laravel permet de generer les interfaces graphiques , avec le langage php 
     @php 
      code php 
      @endphp smithom directif
     avec blade {{hna name dyal variable }}= had joje homa echo f php 
     <a href = {{hna smiya dyal les mehoted ( categories.index)}> zide bootsrap pour le style 
    @foreach ($categories as $c)
    // corps de code 
    <a class href = {{route('categories.show' , ['category(hado rahome homa les parametres )' => $c])}} >show </a>
    <form >
       @csrf ( hadi la couche de security ) 
     f delete dire form wst mno button 
    methode="POST" , @method("Delete") </form>            
    @endforeach 
    creer dans views {
       !n'existe pas dans le cours!  pour creer un composant : components ,
         c'est un fichier  navbar.blade.php 
         kandiro fihe dak code li kayt3awde 
    pour appeler <x-navbar />
    } 
    
    
    



     
 }
