le role d'un controlleur c'est pour controler votre code ou  votre application 
il va appeler la logique du model et donner les resultas a view
pour cree controlleur **php artisan make:controller (Nom de controller ) flag ( --resource ) flag ( --model=Smiyadyal models li deja 3ndna ) **
la logique metie ou backend katkon f dossier app 
il va herite le class selection ( hadchi dans le fichier) {
public function index () {
    recuparation des categories du base de donner , retourner l'interface graphique 
    code :{
        $cats = Category::all(); 
        return view("categories.index" , ["categories"=>%cats]); (compact("cats"))
    }
}
public function create (){
    il va retourner un formulaire sous forme inteface graphique , pour creer categories 
  code : {
          return view("categories.create");
  }
}
 public function store (){
    il va prend les informations et il va stocker dans la base de donner 
    code : {
       $validated = $request->validate(
        [
          "name_category" => "required|string" , 
          "description"=> "required|string"
        ]
       );
       Category::create($validated) on doit ajouter dans le fichier model (
             protected $fillable =["hna ghadi n3tihe ga3e dakchi li ymkn utilisateur ydkhle "] ; 
       )
       retrun redirect()->route("categories.index);
    }
 }
 public function show (){
    il va afficher un category specifier ou selection par vous , sous forme d'un interface graphique 
    code : {
        return view("categories.show" , compact("variable li kayna f parametre dyal had function show ")) ; 
    }
 }
 public function edit (){
    il va  a donner un formulaire bach dire fihome les new informations 
    code : {
    return view("categories.edit", compact("variable li kayna f parametre dyal had function show ")) ; 
    }
 }
 public function update (){
    hna kadire la logique dyal update , katchd dok les informations wa katmchi dirhome f base de donner 
    naffse create 
 }
 public function destroy (){
   il supprimer category dans la base de donner 
   code : {
        $category->delete(); 
        return redirect()->route("categories.index"); (c'est un reponse de l'interface graphique )
   }
   delete deppend de objet , j'ai appeler quand j'ai un objet 
   destroy par id 
 }
}
